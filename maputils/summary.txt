#

fig = eqmap() # Creates EQ Map Figure

fig.add_background('terrain-background', 12) # style, zoom-level
# http://maps.stamen.com/#
# terrain-background : terrain with no labels
# terrain : terrain with labels
# watercolor : watercolor with infrastructure

fig.add_volcano(lat, lon, elev) # (symbol, color, markersize)
fig.add_stations(lat, lon, elev) # (symbol, color, markersize, labels)
fig.eqheatmap(lat, lon, elev) # (colorbar)
fig.eqhypos(lat, lon, elev) # (symbol, color, markersize)
