"""
Waveform and spectrum compute helpers (backend-neutral targets).

Intended future APIs:

- ``prepare_waveform_series`` — time axis + amplitude arrays for a trace or stream
- ``compute_spectrogram`` — frequency/time/power arrays without plotting
"""
