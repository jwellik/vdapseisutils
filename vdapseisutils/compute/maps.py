"""
Map-adjacent pure compute (no matplotlib axes).

Future extractions may include: tick spacing helpers, density grids, hillshade
arrays, and other numeric outputs consumed by ``core.maps`` or Dash.
"""
