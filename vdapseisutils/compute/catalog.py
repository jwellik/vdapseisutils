"""
Catalog-related compute helpers (neutral data prep).

Intended future APIs (migrate from ``core.maps`` / ObsPy plotting paths as needed):

- ``prepare_catalog_points`` — positions, times, magnitudes, sizes for scatter maps
  (neutral successor to matplotlib-oriented ``prep_catalog_data_mpl``).
"""
